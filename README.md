# Job Command Center (encrypted static bundle)

Public hosting shell for a private dashboard. The entire dashboard is AES-256-GCM
encrypted (PBKDF2, 310k iterations); this repo contains only ciphertext.
Source of truth lives in the private repo `jmhan9417/job-command-center`.
